---
# Machine-readable project state. Field semantics: docs/00-context/STATE_SCHEMA.md
goal: "MemeDaily is live on GitHub Pages with custom domain, date-grouped feed, visible sources, and daily target of 10 items"
tier: milestone
scope_in:
  - "Ai-Workflow-Kit collaboration OS files"
  - "MemeDaily product specs and architecture docs"
  - "Static app scaffold, data contract, validation, and publishing automation"
scope_out:
  - "Private platform scraping or logged-in data collection"
  - "OpenAI API integration"
  - "Supabase or server-backed storage"
  - "Real access control beyond public GitHub Pages noindex safeguards"
next_actions:
  - { p: P2, do: "DOMAIN RE-ATTACHED to memedaily.fun (2026-06-29, ICP 备案 approved): Aliyun 9 DNS records re-Enabled (4xA@185.199.108-111.153, 4xAAAA@2606:50c0:800x::153, www CNAME->swyu22.github.io), DoH-verified resolving to Pages IPs; next.config.mjs basePath=''; public/sw.js scope/prefixes now DERIVED from self.registration.scope (no longer hardcoded -> detach/re-attach needs NO sw.js edit); layout.tsx metadataBase+openGraph.url=https://memedaily.fun; public/CNAME=memedaily.fun; Pages custom domain set (gh api PUT cname + CNAME artifact); HTTPS cert valid + enforced. Verified live: https://memedaily.fun 200, www->apex 301, http->https 301, old swyu22.github.io/MemeDaily->apex 301, 备案 footer present. TO DETACH AGAIN if ever needed: next.config.mjs basePath='/MemeDaily' (sw.js self-derives, no edit), layout metadataBase/openGraph.url back to https://swyu22.github.io, remove public/CNAME, clear Pages custom domain, DISABLE the 9 Aliyun records." }
  - { p: P0, do: "Cloud publishing is LIVE and CONFINED: daily-publish (GitHub Actions + claude-code-action SHA-pinned, CLAUDE_CODE_OAUTH_TOKEN, explicit --allowedTools = no arbitrary shell, git in a deterministic post-step) verified green via supervised run 2026-06-22. After a few days of parallel running, DISABLE the local Mac Codex automation (memedaily-daily-publish) to avoid double-publishing" }
  - { p: P0, do: "USER ACTION: rotate/disable the previously-pasted cloud AccessKey in the cloud console (secret-scan gate blocks future leaks, but rotation is external)" }
  - { p: P1, do: "Optional: re-run daily-publish today to regenerate 2026-06-21 under the new content filters (no-news, no-variety-show); current 2026-06-21 was generated before those filters landed" }
  - { p: P2, do: "Budget set to ~$5/day for quality: --max-turns 100 + wide-net multi-pass prompt (more sources: 聚合榜单/平台公开页/梗词典/搜索引擎/外溢; build ~30+ candidate pool, rank, keep best ~10). Cost data points: 80 turns=$4.13/145turns, 40 turns=$1.42/21turns; watch that 100 lands near $5. Deferred: font payload trim; check-state-fresh staleness warning" }
blockers: []
active_session: "ai/sessions/2026-06-20--project-kickoff.md"
updated: "2026-06-29 16:40 +0800"
---

# MemeDaily State Snapshot

## Current Goal
- MemeDaily's governed static scaffold has been pushed and deployed through
  GitHub Pages with `memedaily.fun` and `www.memedaily.fun` DNS configured.

## Active Scope
- Governance layer: `AGENTS.md`, `.cloud.md`, `docs/00-context`, plans, ADRs,
  sessions, hooks, and quality scripts.
- Product layer: data contract, public-evidence collection policy, static pages,
  build validation, GitHub Pages workflows, and Codex automation prompt.

## Last Done
- Created public GitHub repository: `Swyu22/MemeDaily`.
- Evaluated `Swyu22/Ai-Workflow-Kit`: suitable because it is technology-stack
  neutral and provides file-backed state, plan, ADR, session, and hook discipline.
- Imported Ai-Workflow-Kit collaboration OS into this project and normalized
  documentation to lowercase `docs/`.
- Implemented Next.js static scaffold, data contract validation, evidence gates,
  tests, GitHub Pages workflow, skipped-day fallback workflow, and Codex App
  automation `memedaily-daily-publish`.
- Pushed initial commits to `origin/main`.
- Enabled GitHub Pages with `build_type=workflow`.
- Set GitHub Pages custom domain to `memedaily.fun`; root and `www` DNS records
  resolve to GitHub Pages.
- Updated the product UI so detail links use static-page navigation, every card
  displays source evidence, the homepage groups items by date, and the brand header
  shows `W.` plus `WithMedia.与众`.
- Updated today's data file to 10 published items.
- Enforced HTTPS on the custom domain; self-hosted the font stack.
- Centered the header product brand; renamed nav `今日台` -> `首页` (and detail back-link).
- Added an optional `source.title` field (schema + UI renders `title ?? note`) so source
  links can show the real page title, captured at source time by the daily agent.
- Fixed the meme detail share links to use `https://` now that HTTPS is enforced.
- Rewrote `ai/prompts/MEMEDAILY_DAILY_AUTOMATION.md` to curate funnier/remixable memes
  (热梗研究员+趋势编辑+广告创意策略师 lens, A/B/C/D evidence grading, source-title capture)
  and hardened it with explicit JSON-validity invariants (id format, run_report.published
  equality, status semantics, evidence_summary keys) after an adversarial review.
- Recommendation sweep landed: in-code cross-day dedup gate (`crossDayIssues` +
  validate-data + tests), secret-scan gate (`check-secrets.sh` in pre-commit + ci),
  page stale/skipped/partial banner, home-feed pagination, archive lifecycle filter +
  sort, 未评分 chip, ADR-003 (noindex), `daily-monitor.yml` (GitHub-Issue alert), and
  `cloud-fetch-check.yml` (cloud-IP feasibility probe). Deferred font trim with reasons.
- Cloud-IP probe result: aggregators readable from a cloud IP (tophub server-renders a
  full list), but direct Weibo/Xiaohongshu pages hit anti-bot/login walls -> cloud plan is
  aggregator-first.
- Cloud publishing now WORKING end-to-end: claude-code-action needs id-token:write (OIDC),
  the Claude GitHub App installed on the repo, a clean CLAUDE_CODE_OAUTH_TOKEN, and
  --permission-mode bypassPermissions; first green run published 2026-06-21.
- Rebranded to "Trending Today 每日选题参考"; home feed got a heat/freshness sort toggle;
  daily prompt drops pure news and specific variety shows.
- Tab `<title>` set to 热梗日报 (header brand still "Trending Today 每日选题参考" — pending a
  decision on whether to unify).
- Favicon redesigned: dropped the W. monogram for a bold black "T" (Space Grotesk 700 +
  stroke, rendered to raster via Pillow from the self-hosted woff2->ttf) on the brand-yellow
  square. Served under fresh filenames /icon-t.png + /apple-icon-t.png (+ regenerated
  favicon.ico) to beat sticky browser favicon cache; old icon.png/apple-icon.png/icon.svg
  removed. (The .ico was never actually broken — the repeated "wrong favicon" was cache.)
- Budget raised to ~$5/day: --max-turns 100 + wide-net multi-pass prompt (聚合榜单/平台公开
  页/梗词典/搜索引擎/外溢, ~30+ candidate pool). Re-run cost data: 80→$4.13, 40→$1.42,
  100→$3.50. Stricter no-news rule confirmed working (dropped 见义勇为/哽咽 single-event news).
- Added a mobile responsive UI (compact 2-row header, finger-friendly 40px controls, static
  toolbar on mobile, tighter cards, detail-card padding moved to a class, 400px breakpoint);
  verified via phone-width screenshots of home + detail.
- Lifecycle is now a CODE-ENFORCED 10-day rule: `lifecycleIssues` in rules.ts (wired into
  validate, +tests) fails the build if any `declining` item's topic first appeared <10 days
  ago. Re-labeled existing data declining->rising. Prompt updated to match.
- Content filters extended: drop brand/ad-driven memes (王老吉) and celebrity personal-trivia
  (丸子头); strongly prefer new internet slang / 句式 / abstract expressions over
  sports/celebrity/brand. Re-run confirmed: 王老吉/丸子头 gone, all rising/peak.
- Cost note: the wide-net $5/day run came in at $5.89 (token-heavy fetches). --max-turns 100;
  consider 80 if consistently over budget.
- Built `daily-publish.yml`: unattended GitHub Actions cron (23:40 UTC = 07:40 Asia/Shanghai;
  moved OFF the congested 00:00 UTC slot after 06-22 dropped ALL scheduled runs; UI advertises 08:00) running
  `anthropics/claude-code-action@v1` authenticated by a Claude SUBSCRIPTION token
  (`CLAUDE_CODE_OAUTH_TOKEN`, no OpenAI / no separate API billing). It follows the daily
  automation prompt aggregator-first, validates, pushes to main, then dispatches the Pages
  deploy. Guarded to no-op until the secret is added. Also fixed `daily-fallback.yml` to
  dispatch Pages after its bot push (GITHUB_TOKEN pushes don't trigger on:push).

- Card header fix: title block flexes (.head-main), badge+value stay a single non-wrapping
  row (.head-aside) — fixes the misaligned 价值/lifecycle on wide cards.
- Content rules tightened again: drop ALL memes naming a specific brand/company/product
  (被WPS背刺了, 苹果相册乱取名); for people, keep the catchphrase but strip the person name
  from every displayed field (马宁没有软肋全是商务 -> 没有软肋全是商务). Re-run confirmed clean.
- WeChat/OG share card: high-res /share.png (1200x1200, yellow rounded square + bold T) +
  og:/twitter: meta via metadataBase. Note: WeChat caches link previews aggressively.
- 2026-06-22 incident: GitHub dropped ALL scheduled runs (publish/fallback/monitor) — no
  content shipped. Manually dispatched daily-publish (workflow_dispatch is reliable) -> 06-22
  came out partial/5. Hardened the pipeline: (1) new `daily-catchup.yml` runs 09:10/11:10/13:10
  Asia/Shanghai (cron "10 1,3,5 * * *") and DISPATCHES daily-publish if today's envelope is
  still missing & no publish is active — survives schedule drops since dispatch is reliable;
  (2) moved `daily-fallback` (skip-marker) to 21:20 (evening last resort, can't pre-empt
  catch-up); (3) moved `daily-monitor` alert to 14:30 (after the catch-up window). Residual
  risk: a very bad GitHub day could drop the catch-up schedules too (no external pinger by design).
- Cost guard on daily-publish: a `schedule`-triggered run no-ops (green, ::notice) if today is
  already `published`, so a late-firing cron can't double-spend after a manual/catch-up publish.
  Only the `schedule` event is guarded — workflow_dispatch (manual + daily-catchup) always runs,
  and a `partial` day can still be upgraded (as 06-22's 5->9 was). Guard wired via steps.guard.outputs.skip.
- Skills-driven full audit (9 dims, adversarial verify): 32 findings confirmed / 11 rejected. Fixed 28:
  HIGH — fixed basePath nav bugs in MemeCard+ArchiveClient (raw <a> 404'd on /MemeDaily subpath),
  06-20 generated_at temporal fix, anti-prompt-injection clause in MEMEDAILY_DAILY_AUTOMATION.md.
  MED/LOW — secret-scan hardening (-Ena, fail-closed, +Google/Slack patterns), check-file-size awk,
  fetch-fonts trap, daily-fallback rebase-before-push, rules.ts DRY (itemNames) + new temporal gate,
  +4 gate tests (19 total), noUncheckedIndexedAccess, removed dead dep @vitejs/plugin-react, data.ts
  memoize, CopyButtons try/catch, single-h1 outline, dead CSS removed (.callout/.chip/.font-*), CSS dedup,
  README/PROJECT_MAP/ADR-001 staleness, ADR-004 (gate-complexity exception). All lint/validate/typecheck/
  test/build green. DEFERRED (need human/supervised): [1b] claude-code-action --allowedTools / move git
  out of agent (supervised run); [2] SHA-pin all actions; [10] product-spec line; [12] wire complexity
  ESLint rules (would emit ~21 warnings vs --max-warnings=0).
- All 4 deferred audit items RESOLVED + verified: [1·P2] daily-publish agent CONFINED (no
  bypassPermissions; explicit --allowedTools => only Bash(npm run check); token out of shell env via
  HAS_OAUTH boolean; checkout persist-credentials:false; commit/push/deploy in a deterministic
  post-step; date via step output) — SUPERVISED dispatch ran green end-to-end, NO tool denials,
  confined agent produced 10 published items, post-step published + deployed (site 200). [2]
  claude-code-action SHA-pinned (2fee155 # v1) + dependabot.yml. [10] product-spec -> cloud-primary.
  [12] advisory `npm run lint:complexity` (eslint.complexity.config.mjs) + `npm run check` aggregate.
  Net: the audit's sole HIGH residual is closed; daily publisher confirmed working under confinement.
- Partial-day notice copy updated: adds "鉴于合格内容较少，当日发布可能少于每日目标".
- 2026-06-23 reliability + quality hardening (big day):
  - Refactored daily-publish to a CONFINED agent (no bypassPermissions; --allowedTools; token out of
    shell env; persist-credentials:false; deterministic post-step does commit/push/deploy). Found+fixed
    two real bugs it exposed: agent sometimes finished without writing the file (now prompt hard-requires
    Write + post-step fails loud if no file); and `git diff --quiet` missed NEW untracked day-files so the
    first publish of every day silently no-op'd (now `git add` then `git diff --cached`). Same bug fixed in
    daily-fallback.
  - Reliable trigger = cron-job.org @ 07:00 (external cron -> GitHub workflow_dispatch API, minimal PAT
    actions:write) — verified working. GitHub schedule moved to 07:00 (backup); daily-catchup -> hourly
    08:00–13:00; daily-monitor 14:30. Claude /schedule routine REJECTED (it's app-local, needs Mac open).
  - UI 发布时间 now shows real generated_at (not fixed 08:00).
  - Politics: hard-drop rule in prompt(s) + CODE-ENFORCED `politicalContentIssues` gate in rules.ts
    (high-signal subject terms; narrowed to avoid false positives) — fails validate so the agent self-
    corrects. Removed a live political item (larry-the-cat UK-PM).
  - Speed: scripts/prefetch-hotlists.sh parallel-fetches aggregators -> prefetch/*.txt (agent Reads,
    no slow sequential WebFetch); agent self-checks with fast `npm run validate` not full build; turns 60.
  - Dedup lock (skip any trigger if today published, force=true overrides) + cost-runaway alert (>4 runs/18h).
  - Cost note: 06-23 burned ~$12 chasing the above bugs (3-4 runs); normal is ~$4/day, 1 run.
- 2026-06-23 supervised end-to-end test of the new flow (forced run, run 28006874611): ALL steps
  green. dedup-lock honored force=true; prefetch step success (CI yield: tophub 48KB + baidu 20KB +
  moyuoo 6KB ≈ 75KB; rebang/cnxiaobai/weibo empty = JS-rendered/anti-bot, agent WebFetches those);
  confined agent wrote 9 items (no missing-file regression); post-step committed the (force-overwritten)
  file + deployed. Result: 06-23 published 9, ZERO politics residue, real generated_at shown (14:00).
  SPEED: total ~27.5 min (06:27→06:55) vs ~50 min before (~45% faster) — prefetch (2s for the pool) +
  fast `npm run validate` self-check (no 90s build). 07:00 start + ~27min => ready ~07:30, before 08:00.
  No issues found. Deferred idea (not done, user said leave it): parallelize the agent's verification
  to trim the ~27min research without cutting breadth.
- 2026-06-24 梗库/卡片 UI 三项 (committed): (1) MemeCard「连续 N 天」仅在 days_on_list>=2 时显示
  (去掉每卡都有的「连续1天」噪音)，数字红色(--red-fg #dc2626)+1.15x 加粗，highlight 真正持续多日的梗。
  (2) 梗库 archive 去重+复合排序：rules.dedupeRecurring(先按 id、再按归一化 title/alias)合并跨天复现的同一个
  梗(如 rise-to-fired 06-22→06-23 只保留最近一次 06-23)，默认排序改为 labels.sortByDateThenLife = 日期降序
  → 正热>还能上车>已过气(archive 专用 peak-first，区别于首页 freshness 的 rising-first) → 热度值(score)。
  (3) 列表文字过长不再挤压标签：.result-main{flex:1;min-width:0} 让摘要在自己列换行；.tag-row.head-aside
  与 .result-row>.tag-row 强制 nowrap 让生命周期/价值标签单行(修了 .tag-row flex-wrap:wrap 覆盖 .head-aside 的 bug)。
  +2 dedup 单测(共 26 全绿)，npm run check 绿，静态预览实测三项均正确(梗库 39→38 行去重、排序符合预期、连续2天红色大号)。
- 2026-06-24 两项 (committed): (1) 连续标签文案：第1天=「第 1 天上榜」数字不标红/不加粗(去掉 streak-n)；
  ≥2 天=「连续 N 天上榜」数字仍红色大号加粗，3 天+以此类推 (MemeCard.tsx 三元: >=2 styled / ==1 plain / else null)。
  (2) 修复移动端「每天第一次打开显示过时内容且无 CSS」: 根因是 GitHub Pages/Fastly 给 HTML 和内容哈希资源
  统一 cache-control:max-age=600 且无法自定义 header；每日 07:00 部署替换整站 artifact、清掉旧哈希 chunk，移动端
  首开复用了过期缓存的 HTML(或 bfcache)，其引用的旧 CSS 哈希已被清除→404 无样式、内容是昨天的，手动刷新才好。
  方案(经 workflow 三方案设计、本地人工对抗复核——云端对抗 agent 因本地代理证书错误失败)：新增 public/sw.js
  网络优先 SW——导航(HTML)network-first(cache:'no-store' 旁路 CDN 缓存，在线必拿最新 HTML→同时治好"过时"和"丢 CSS"，
  离线才回退缓存)；/_next/static 不可变哈希资源 cache-first(消除 10 分钟重拉、躲过清缓存竞态)；fonts SWR；data/其他
  透传。+ src/app/ServiceWorkerManager.tsx (client) 注册 + 每次 load reg.update() + 新 SW 即时 skipWaiting 接管 +
  controllerchange 单次 reload(救活首开过期页) + pageshow(event.persisted) bfcache 兜底(>30min reload)。layout.tsx
  挂载。"stale-forever 结构性不可能"(HTML 永不优先取缓存)；killswitch 内建于更新通道(可随时发自注销 sw.js 全量生效)。
  本地预览实测：SW activated/controlling、scope /MemeDaily/、缓存 html+assets(45) 已建、无报错、无 reload loop、
  标签与样式正确。注：GitHub Pages 用 artifact 部署不跑 Jekyll，.nojekyll 非必需(未加)。
- 2026-06-25 Skills 全量审计 (NOT yet committed — 待 user review diff): 8 维度并行审查 + 逐条对抗验证
  (gha-security/agentic-actions-auditor、next、typescript、react、code-review、web-perf、agents-md、
  claude-settings-audit)。29 findings：3 证伪(GHA-1 Bash 精确匹配非前缀/DEDUP-1 注释经数据核实正确/
  FONT-1 经我 fonttools+canvas 复核=可变字体加粗正常，推翻 workflow 误判)。已修 18 项(13文件)：
  PKG-1 check 加 lint；TS-1 删 3 死导出(data.ts)；TS-2 守卫日期 parts；TS-3 启用类型感知 lint
  (eslint-config-next/typescript，并修了它发现的 sw.js 未用参数)；SW-1 首装不再 reload(hadController)；
  SWM-1 bfcache 守卫无条件安装；SW-2 删离线首页回退；SW-3 注释更正；POL-2 政治门禁加扫 risk.note；
  DEDUP-2 注释记录静默删行；GHA-1 注释 check→validate；NEXT-1/2 metadataBase 改 github.io 源(OG 预览图
  现可达)；README-1/2/3 + 产品方案 PLAN-1 文档同步现状(子路径/ICP/SW)。GHA-2/3 已做 daily-publish.yml
  双 job 拆分(agent job contents:read+id-token，产物 artifact → publish job contents:write 检出可信代码
  +npm run check+commit/push) → 受网页操纵的 agent 不再持写权。验收：npm run check 全绿(lint 类型感知/24 测/
  build)、SW 浏览器实测 activated+网络优先导航+缓存优先资产+0 报错、workflow YAML 结构校验通过、
  OG 标签实测解析到可达 github.io URL。**需人工确认未做**：POL-1 收紧政治子串词(降召回 vs 零例外取舍，
  我建议保守不动)。其余可选整洁项(CPB-1/ARC-1/TDF-1/NEXT-3/CSS-1/FONT-3)已文档化未做。
- 2026-06-25 发布时间显示修复: 前台「发布时间」原读 generated_at(agent 猜的、无可靠时钟，常比真实推送晚
  1-2h——如 06-24 显示 09:00 但真实 git commit=07:35+08:00，早起用户会看到"未来"的发布时间)。改为新增
  schema.published_at(可选、+08:00)由**确定性 publish 步骤**盖真实推送时刻(Asia/Shanghai 固定 UTC+8)；
  前台 page.tsx 显示 published_at ?? generated_at。**不能直接改 generated_at**：实测 24 条 captured_at 中 16 条
  晚于真实推送时刻，下调 generated_at 会破坏 captured_at<=generated_at 门禁——故 published_at 独立、保留
  generated_at/captured_at 及其门禁不动。daily-publish.yml publish job 在 npm run check 前 node 盖戳；
  create-skipped-day.ts 同步设 published_at；回填 06-24.json published_at=07:35:56(真实 commit 时刻，单行 surgical
  插入不重排)。验收：npm run check 全绿、构建 HTML 与浏览器实测「发布时间 07:35」(非 09:00)。
- 2026-06-25 移动端"打开仍无 CSS/散内容"复发的根治 (workflow 7 agent 枚举失败模式+对抗验证): 根因——陈旧
  缓存 HTML 引用已被清除的哈希 CSS→404→无样式，且同一陈旧 HTML 的哈希 JS 也 404→React 永不 hydrate→
  之前放在 React 组件(ServiceWorkerManager)里的 SW 注册/兜底在"无样式"场景下根本不执行；审计的 SW-1 又移除了
  首装 reload 救活。修复：删除 ServiceWorkerManager.tsx，改为 layout.tsx <head> 内联 boot 脚本(不依赖任何外部
  chunk，必定执行)：(1) 自愈——若布局关键 /_next/static 样式表 cssRules 为空(实测 sheet===null 不可靠！404 表
  仍有空 sheet，必须用 cssRules.length===0)，带唯一 _r 缓存破坏参数 reload 取最新 HTML；有限重试(≤2 次/60s,
  浏览器实测确认绝不死循环)，成功后 history.replaceState 去掉 _r；(2) pageshow(persisted) 覆盖 bfcache(window.load
  在 bfcache 恢复时不触发)；(3) 注册网络优先 SW(iOS 会回收 SW，故每次 load 重新注册)。sw.js 加固：handleNavigation
  用 htmlKey() 去 _r 再做缓存键；handleImmutableAsset 只缓存 fresh.ok&&type==='basic'(绝不缓存 404 污染)。验收：
  npm run check 全绿(24 测)、浏览器实测——正常页不误触 reload(无 _r/budget)、注入 0-rule 样式表能被检出、有限重试
  最多 2 次即停、SW activated、0 console error。缓存破坏 ?_r=唯一时间戳 经线上实测命中 origin 取最新 HTML。
- 2026-06-28 修复"内容已生成但没上线"的部署竞态: 现象——06-28 内容已 commit 进仓库(560f2c2, published 9 条,
  published_at 07:34 由新 publish job 自动盖戳)，但线上一直显示 06-27。根因: GITHUB_TOKEN 的 push 不触发
  on:push，故 publish job 用 `gh workflow run pages.yml --ref main` 手动部署；但 push 后立刻 dispatch，main HEAD
  尚未传播到 dispatch 解析器→部署跑在了 PRE-push 的旧 commit(879dc54=06-27)上，560f2c2(06-28) 从未被部署。
  即时处置: 手动 dispatch pages.yml(此时 main 稳定=560f2c2)→ 06-28 上线(线上实测 2026-06-28 发布时间 07:34)。
  根治: publish job 部署段改为 sleep 10 → dispatch → sleep 6 → 校验最新 pages run 的 headSha==推送的 SHA，
  不符则重试(最多 4 次)，全失败则 ::error 退出(让 catchup/monitor 介入)。daily-publish.yml YAML 结构校验通过。
  注: 这是"绿色但没上线"的部署侧故障，与之前修的"客户端陈旧缓存无样式"不同——两者都已分别根治。
- 2026-06-28 内容质量收紧(用户反馈: 推送很多更像社会新闻/趣味故事而非网络流行梗): 根因——agent 把
  "有画面感/反差/好笑"当成够格梗壳，放进了病毒式趣味观察/段子/视频标题(如"奶奶的微信77万未读""3000块
  街舞课狗狗学了2900""苦味是它在警告你别吃")。这些不是「网络流行语」(大众能自己造句套用接龙的语言单位)。
  修复: 同时收紧 ai/prompts/MEMEDAILY_DAILY_AUTOMATION.md(agent EXACTLY 读) 与 daily-publish.yml 内联 prompt——
  (1) 顶部门槛重写: 每条必须是「可复用语言单位」(句式/口头禅/谐音/抽象表达/可填空模板)，热度/好笑/画面感
  不能替代可复用性；(2) 新增硬规则「不要病毒式趣味观察/段子/视频标题」+「造句/迁移测试」(陌生网友能把这句
  原样/套壳用到完全无关的事上才是流行语，只能复述那个故事就丢)，并把用户三个例子列为反例；(3) 提炼指令:
  title/summary 要写大众在用的"那句话/句式"本身、不是新闻式梗概，沉淀不出通用可套用的话就丢。无代码门禁
  (新闻 vs 梗是语义判断、无可靠关键词，硬加会误伤如"父爱如山体滑坡")，靠 prompt 收紧。仅影响后续 agent 运行
  (06-29 起自动生效)；今天 06-28 是否 force 重跑应用新规则、待与用户确认。
- 2026-06-29 页脚备案/版权: layout.tsx 全站底部加居中 <footer.site-footer>——粤ICP备16102279号(链到 beian.miit.gov.cn/#/Integrated/index, target=_blank rel=noreferrer noopener) + Copyright © 2011-2026 深圳市与众文化传播有限公司 版权所有；globals.css 加 .site-footer 居中样式。check 全绿、浏览器实测居中且链接正确。
- 2026-06-29 域名回挂 memedaily.fun (备案通过): (1) 阿里云 9 条 DNS 记录全部 SetDomainRecordStatus->Enable (4xA@185.199.108-111.153, 4xAAAA@2606:50c0:800x::153, www CNAME->swyu22.github.io)，DoH 实测已解析到 Pages IP；(2) 仓库回根路径：next.config.mjs basePath="", public/sw.js 前缀改为从 self.registration.scope 派生(永久消除锁步隐患，不再硬编码/MemeDaily/), layout.tsx metadataBase+openGraph.url 改回 https://memedaily.fun, 新增 public/CNAME=memedaily.fun;(3) GitHub Pages 自定义域名=memedaily.fun(CNAME 工件 + gh api)。本地 basePath="" 构建+浏览器实测：资产走根路径、SW 根 scope activated、CSS/footer 正常、OG->memedaily.fun。HTTPS 证书 GitHub 自动签发(可能滞后数分钟~小时)，证书就绪后再开 enforce HTTPS。robots 仍 noindex(未改，如需被收录再说)。
## Next Actions
1. P0: Run `cloud-fetch-check` (Actions → Run workflow) to confirm a cloud IP can read the
   public CN aggregator/platform pages. If yes, stand up a Claude `/schedule` routine
   running `MEMEDAILY_DAILY_AUTOMATION` and retire the local-Mac dependency. Note Pro is
   sufficient; routine billing as of mid-2026 is in flux — verify in your own account.
2. P0 (user action): rotate/disable the previously-pasted cloud AccessKey in the console.
   The secret-scan gate now blocks future leaks, but rotation must happen externally.
3. Done this session: cross-day dedup gate, secret-scan gate, page stale banner + GitHub
   Issue monitor, home pagination, archive lifecycle filter/sort, noindex ADR.

## Open Questions / Risks
- GitHub Pages on GitHub Free requires a public repository. This repo must not
  contain secrets, private customer information, platform cookies, or proprietary
  internal strategy.
- Codex App automation depends on the current Mac staying awake, connected, and
  logged in.
- Public-web evidence can be incomplete or unstable; publication gates must prefer
  skipped/partial days over weak or unsafe content.

## Related Files
- Product plan: `产品方案.md`
- Design handoff: `docs/project/`
- Active plan: `docs/20-plan/current-iteration.md`
- Active session: `ai/sessions/2026-06-20--project-kickoff.md`
