---
# Machine-readable project state. Field semantics: docs/00-context/STATE_SCHEMA.md
goal: "Make three items a recovery floor rather than an editorial target, enforce a domestic-majority DailyNews mix, and prove the corrected Cloud pipeline end to end"
tier: milestone
scope_in:
  - "Editorial-completeness contracts for both feeds, including full candidate selection up to ten and second-pass proof when only three qualify"
  - "DailyNews domestic/international scope, a domestic share of at least 75%, and representative international-news evidence"
  - "One-time migration of today's legacy envelopes through the trusted exact-file publisher"
  - "Eight ChatGPT Work Web Scheduled Tasks for MemeDaily and DailyNews primary, catch-up, monitor, and fallback modes"
  - "Retirement of the eight mistaken local Codex heartbeat automations"
  - "2026-07-26 dual-feed backfill, trusted candidate publication, Pages correlation, production alignment, and operational evidence"
  - "Minimum-three availability semantics, bounded editorial recovery, monotonic under-minimum repair, and a real 2026-07-26 meme repair publication"
  - "2026-07-26 meme editorial correction and post-2026-07-27 dynamic score/activity/identity gates with no fixed cross-day quota"
scope_out:
  - "Unrelated visual redesign, historical audit rewrites, private-platform extraction, and paid model APIs in the product"
  - "Destructive external credential changes or private account administration unrelated to the takeover"
next_actions:
  - { p: P1, do: "USER: revoke/rotate the previously exposed Aliyun AccessKey outside this repository" }
  - { p: P2, do: "USER: refresh/reinstall the iOS Chrome Home Screen app and confirm the dark-mode status area on device" }
blockers:
  - "Aliyun credential rotation and physical-device iOS confirmation remain external manual actions"
active_session: "ai/sessions/2026-08-01--editorial-completeness-and-domestic-news.md"
updated: "2026-08-01"
---

# MemeDaily State Snapshot

## Current Goal

- Keep the owner's Mac outside the availability path while separating availability
  (`>=3` visible items) from editorial completion. A normal run must finish discovery,
  publish every selected-tier qualifier up to ten, and prove a second research pass
  before concluding that only three items qualify.
- DailyNews must classify geography independently from topic, keep China-centered
  everyday-life news at or above 75% of the visible set, and admit international news
  only when it is broadly representative or materially relevant to Chinese readers.
  Routine local news from any foreign country is out of scope; no country-specific ban
  is intended.

## Reconciled Baseline

- The accepted 2026-08-01 content state is live at main
  `79a12fbda147b2c7252e1e9e5cf67e17cf7e3c4d`: six memes under
  `v4-editorial-completeness` and five news items under
  `v3-domestic-majority`, all five news items domestic. The closeout change is
  documentation-only and must remain a descendant of that content state.
- The completed dynamic policy is merged on remote `main`
  `e1dee582ff97da9997497dd901954a2b1e7efd4c`.
- PR #48 replaced the unacceptable all-prior-day meme board with four reverified
  items. Data-only PR #49 then added a distinct, semantic leading emoji to each title
  without changing identity, evidence, ordering, or counts.
- PR #37 merged the takeover at `fbccae3`; its main CI and Pages run succeeded.
  PR #38 merged the final wait-budget/spec correction at `3dc921b`; its main CI and
  Pages run succeeded.
- On 2026-07-26, neither feed had a current-day envelope and GitHub had no candidate
  branch, PR, or Actions run. The eight supposed Cloud triggers were found to be local
  Codex Desktop `heartbeat` automations pointing at Cloud contexts, so they had not run
  while the local scheduler was unavailable.
- The eight local heartbeats were deleted and replaced with eight genuine ChatGPT Work
  Web Scheduled Tasks. Recovery PR #40 published the meme `skipped` envelope at
  `6583aec`; PR #41 then published six news items at final main `90aa02c`.
- The owner rejected zero-item days, then rejected the first minimum-three repair because
  all three items duplicated 2026-07-25. PR #48 corrected the live board to four items.
  One selected phrase had a real same-day second wave, proving the desired rule is
  evidence-based recurrence rather than either forced novelty or a carry-over cap.

## Current Architecture

- Next.js static export serves two git-backed feeds at `https://memedaily.fun`:
  memes in `data/daily/` and everyday-life news in `data/daily-news/`.
- Eight ChatGPT Work Web Scheduled Tasks own unattended schedules and read current
  rules/data from GitHub on every run. They cover news at 06:00, hourly 07:15–12:15,
  14:45, and 21:30, and memes at 07:00, hourly 08:00–13:00, 14:30, and 21:20, all in
  Asia/Shanghai. They submit one exact daily JSON on
  `codex/daily-{meme|news}-YYYY-MM-DD` and open a same-repository PR. Their eight
  persistent prompts were updated and directly read back from Scheduled Tasks. A cheap
  terminal no-op requires the exact current feed policy, `published`/`partial`, three
  to ten evidence-qualified items, matching reported/visible counts,
  `editorial_complete: true`, and the complete feed contract. Legacy policy is
  migration work, `held` is operator-only, and fallback cannot publish `skipped`.
- Codex Desktop heartbeat automations are not Cloud schedules and are not part of the
  availability path. The repository contract requires every fixed catch-up to start
  with an inexpensive live-main completion preflight and stop before research/mutation
  only for a current-policy, editorially complete, fully valid three-to-ten-item
  envelope; updating all eight external persistent prompts to that contract is
  complete.
- `codex-daily-pr-publish.yml` never checks out the PR tree. A read-only job fetches
  only the JSON blob and validates with trusted main code; a separate trusted job
  stamps, revalidates, rebases, pushes, and waits for correlated Pages success.
- Repository ruleset `codex-trusted-main` is the credential boundary: Cloud-connected
  tools and ordinary users cannot update/merge main; one Actions-secret-backed write
  deploy key is the sole bypass and appears only in trusted final push steps.
- Old Anthropic publishers, GitHub cron, catch-up dispatchers, and external cron
  ownership are retired. Fallback/monitor workflows are manual disaster recovery.
- Public-web content is untrusted data. No login cookies, private APIs, anti-bot
  bypass, copied media, private data, or paid model API is part of the product.

## Implemented Hardening

- Production dependency audit is zero after the required Next/PostCSS/Sharp updates;
  CI and Pages enforce the production high-severity gate under Node 22/npm 10.
- Pages build has read-only contents permission; only deploy receives Pages/OIDC writes.
- Pages correlation adopts the deploy-key push run when it appears, falls back to one
  explicit dispatch only when needed, retries transient failures, and returns success
  only for a run covering the accepted SHA (or descendant) with conclusion `success`.
- Its 75-minute per-run correlation budget and 180-minute writer budget cover the
  configured 35-minute Pages job maximum, one predecessor, and one bounded recovery.
- Fallback uses live-main sync, strict date/path containment, shared writer
  concurrency, post-rebase full checks, and an explicit `HEAD:main` push.
- Monitors read live main and verify Pages freshness; empty DailyNews input now fails.
- Reader client payloads use a public meme projection; internal editorial fields remain
  server/repository only. Detail pages now expose canonical/OG/Twitter metadata.
- ADR-007 and `ai/prompts/CODEX_CLOUD_RUNBOOK.md` define the active operational contract.
- ADR-008 introduced the minimum-three availability state, bounded recovery ladder,
  and one monotonic trusted repair exception for an exact under-minimum daily file.
- ADR-009 defines the post-2026-07-27 meme score/activity model: at least 30 candidates,
  no fixed cross-day quota, stable full-history recurrence identity, post-publication
  activity, auditable candidate outcomes, strictest-sufficient tiers, Top-N capacity,
  and privacy-minimized safety drops.

## Verification Status

- Editorial-completeness PR #65 merged at `4a2849042eb7dd5efb2904cdc635232ba79a8be1`;
  main CI `30692575469` and Pages `30692575463` succeeded, and the ruleset was restored
  active with only its DeployKey bypass. The first real migration candidate also found
  and corrected a fallback regression that had depended on today's production file
  remaining permanently on a legacy policy instead of constructing its own fixture.
- Regression-fix PR #66 merged at `995c947843914cf300c8e8051ff3aed0c3fe9ef3`;
  its checks and the resulting main CI/Pages runs succeeded. The fallback test now
  constructs an explicit legacy-policy fixture and no longer depends on the live daily
  file remaining legacy.
- Exact meme candidate PR #67 was accepted by trusted publisher run `30693058662`,
  which published six strict qualifiers from 33 audited candidates at main
  `198f7a443bb8d48492ef0c7a1351771fd9e81b1c`, closed the non-draft candidate PR,
  and correlated successful main CI `30693137149` and Pages `30693137142`.
- Exact news candidate PR #68 was accepted by trusted publisher run `30693387402`,
  which published five strict qualifiers from 40 audited candidates at main
  `79a12fbda147b2c7252e1e9e5cf67e17cf7e3c4d`, closed the non-draft candidate PR,
  and correlated successful main CI `30693471863` and Pages `30693471858`. The final
  mix is five domestic and zero international items; zero international is valid and
  no routine foreign-local or space-news padding was used.
- A cache-bypassed production request returned HTTP 200 with a cache miss and all
  eleven accepted titles. Live classification is `terminal` for both feeds: meme
  `published/6` with policy `v4-editorial-completeness`, and news `published/5` with
  policy `v3-domestic-majority` and a 5/0 domestic/international mix.
- The final data tree passes both validators, strict lint, typecheck, all 257 tests,
  a 197-page static build, production dependency audit with zero findings, strict
  governance/security gates, and `git diff --check`.
- On 2026-08-01, the meme catch-up and news catch-up ChatGPT Work Scheduled Tasks were
  each manually invoked from the server task UI after publication. Their Cloud run
  reports classified the exact current-policy envelopes as terminal, returned
  `no-op`, and explicitly stopped before living-rule/schema/history reads, research,
  writes, branches, PRs, issues, merges, or task edits. Post-run GitHub readback showed
  unchanged main `79a12fb`, unchanged exact daily branch tips (`afd6a19` meme and
  `d3f5562` news), unchanged closed PR #67/#68 records, and the active
  DeployKey-only ruleset.

- Eight genuine ChatGPT Work Web Scheduled Tasks are active with the exact
  Asia/Shanghai primary, catch-up, monitor, and fallback cadence; the eight mistaken
  local heartbeat automations were deleted.
- The takeover-era Node 22.23.1/npm 10.9.8 baseline passed production audit,
  validators/lint/typecheck, 115 tests, and a 176-page build; the current final suite
  is the 257-test/197-page result recorded above.
- Strict all-repository and staged governance gates, workflow YAML parsing, shell
  syntax, diff whitespace, and 28 focused security/reliability/data tests pass.
- All retained workflow YAML parses and `scripts/dispatch-pages.sh` passes shell syntax.
- The takeover-era independent subtask verification passed typecheck, strict lint,
  113 tests, a 176-page static build, production audit, governance checks, and
  targeted output inspection before that integration.
- Ruleset `codex-trusted-main` was activated after PR #37 and API-verified with only
  a DeployKey bypass, restored after PR #38's bounded maintenance window, and verified
  active again with exactly one writable repository key.
- Main CI run `30162830895` and Pages run `30162831002` succeeded for `3dc921b`;
  a cache-bypassed production request returned HTTP 200 and current 2026-07-25 content.
- Under the superseded pre-ADR-008 contract, a formal `news`/`fallback` ChatGPT Work
  Cloud run read live main `3dc921b`, treated that day's valid `skipped` envelope as
  terminal, and returned no-op without any branch, PR, comment, or main mutation.
- The unused `CLAUDE_CODE_OAUTH_TOKEN` was deleted after confirming no active workflow
  references Anthropic; only `CODEX_PUBLISH_DEPLOY_KEY` remains.
- Meme recovery PR #40 produced a valid `skipped` envelope with zero items. Trusted
  publisher run `30186891155` accepted it at main `6583aec`; CI `30186948953` and
  correlated Pages run `30186948929` succeeded.
- News recovery PR #41 produced a valid `published` envelope with six items. Trusted
  publisher run `30186938154` accepted it at final main `90aa02c`; CI `30187028830`
  and correlated Pages run `30187028839` succeeded.
- Under the interim 2026-07-26 contract, the 12:00 meme Web Scheduled catch-up showed
  `Last ran 12:02`, read the then-terminal live-main `skipped` envelope, and returned
  no-op. The 12:15 news catch-up then ran at 12:14, read the terminal `published`
  envelope with six items, and also returned no-op. Neither run created any additional
  GitHub mutation.
- A cache-bypassed production request returned HTTP 200 with both 2026-07-26 envelopes
  and `Last-Modified: Sun, 26 Jul 2026 04:00:00 GMT`.
- The integrated minimum-three tree passes both validators, strict lint, typecheck,
  158 tests, and a 176-page static build. The staged rollout deploys the publisher repair
  boundary before submitting the three-item meme repair and activating the final domain
  cutoff, preventing the existing zero envelope from becoming immutable.
- Stage-one PR #43 merged at `6226096`, but independent post-merge review caught an
  indented-heredoc Bash parse failure plus held/count/fallback edge cases before any
  repair candidate was submitted. PR #44 fixed those issues at main `4c8ac36`; its CI
  run `30190726823` and Pages run `30190726797` succeeded, and the ruleset was restored
  active with only the DeployKey bypass.
- PR #45 changed the fallback regression to a self-contained two-item fixture; CI,
  Pages, 146 tests, a 176-page build, and independent review passed.
- Exact candidate PR #46 was accepted by trusted publisher run `30191367788`, which
  committed `ce1c961` to `main`, waited for correlated Pages run `30191428317`, and
  closed the PR. GitHub main and cache-bypassed production now show meme `partial/3`
  and news `published/6`.
- All eight Scheduled Tasks were directly read back as `ACTIVE`, with unchanged
  Asia/Shanghai schedules and correct feed/mode/path bindings. Each persistent prompt
  now requires exact current policy, `editorial_complete: true`, three-to-ten qualified
  items, full validation, operator-held no mutation, fallback recovery without
  `skipped`, and terminal cheap no-op.
- All four meme Scheduled Tasks were then updated and read back after PR #50. Each
  persistent prompt now contains the eight dynamic-selection clauses: no fixed
  cross-day quota, full-history identity, 30–100 candidate audit, strictest-sufficient
  tier, Top-N/capacity, privacy-minimized safety audit, post-publication recurrence
  activity, and semantic leading emoji. No task, status, schedule, timezone, next-run
  cadence, or Custom recurrence structure changed.
- The final domain rules in PR #47 reject post-cutoff `skipped` and under-three
  envelopes for both feeds while preserving the operator-only `held` emergency state.
- PR #48 replaced the all-prior-day repair with four independently reviewed items at
  main `73d9543`. Main CI `30202300031` and Pages `30202299980` succeeded; production
  returned HTTP 200 and displayed all four corrected titles.
- PR #49 added `✨`, `🫥`, `🔄`, and `🤝` to those four titles at main `6dc4cc8`.
  Main CI `30204360069` and Pages `30204360045` succeeded, the ruleset was restored
  active with only the DeployKey bypass, and a cache-bypassed production read showed
  the emoji titles.
- The dynamic-policy tree passes both validators, strict lint/typecheck, all 191
  tests, a 180-page static build, production dependency audit with zero findings,
  all strict governance/security gates, and focused score/identity/audit/UI tests.
  The only 44 complexity warnings are pre-existing outside the newly introduced
  dynamic-selection and label code/tests.
- Final independent review returned GO after reconciling id-reuse and optional per-source
  `observed_at` wording. It confirmed there is no active fixed recurrence count, ratio,
  or identity-age cap and no remaining prompt/schema/validator mismatch.
- Dynamic-selection PR #50 merged at `e1dee582ff97da9997497dd901954a2b1e7efd4c`.
  Main CI `30204833745` and Pages `30204833760` succeeded. A cache-bypassed
  production read still displayed all four semantic-emoji titles, and repository
  ruleset `codex-trusted-main` remained active with only its DeployKey bypass.

## Persistent Risks

- Connected GitHub tools retain branch/PR permissions, but the main ruleset blocks
  their direct-main/self-merge path. The trusted candidate publisher and genuine
  server-scheduled no-ops for both feeds were verified on 2026-08-01; observing the
  next naturally content-producing scheduled candidate remains useful defense in
  depth.
- Fixed catch-up tasks still incur one Web Scheduled invocation and inexpensive
  live-main preflight after an earlier complete result. They then stop without research,
  data writes, branch creation, or PR creation. Under-minimum results deliberately
  continue into bounded research and may consume more tokens until three items are live.
- Public-source semantics require editorial judgment; deterministic gates cover
  high-signal failures but cannot prove every contextual decision.
- A previously shared Aliyun AccessKey must be revoked/rotated outside the repository.
- Final iOS installed-PWA status-area rendering still needs physical-device confirmation.
- GitHub Pages and this public repository are never private storage.

## Canonical Pointers

- Active plan: `docs/20-plan/current-iteration.md`
- Project map: `docs/00-context/PROJECT_MAP.md`
- Cloud runbook: `ai/prompts/CODEX_CLOUD_RUNBOOK.md`
- Living rules: `ai/prompts/MEMEDAILY_DAILY_AUTOMATION.md` and
  `ai/prompts/DAILYNEWS_DAILY_AUTOMATION.md`
- Decision: `docs/30-decisions/adr-007-codex-cloud-candidate-publishing.md`
- Dynamic selection: `docs/30-decisions/adr-009-dynamic-meme-selection.md`
- Editorial completeness and domestic-majority decision:
  `docs/30-decisions/adr-010-editorial-completeness-and-domestic-majority.md`
- Active session: `ai/sessions/2026-08-01--editorial-completeness-and-domestic-news.md`
