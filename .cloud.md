---
# Machine-readable project state. Field semantics: docs/00-context/STATE_SCHEMA.md
goal: "Harden the dual-feed publish pipeline against agent-output and dispatch flakiness, then observe the next scheduled writers"
tier: feature
scope_in:
  - "Next scheduled meme/news writer runs, monitors, fallback behavior, and production feed freshness"
  - "Physical iOS Chrome dark-mode PWA confirmation and rollback evidence"
  - "Audit residual-risk tracking and external credential reminder"
scope_out:
  - "New product features, unrelated visual redesign, and historical design-export rewrites"
  - "Dependency upgrades without a security or correctness requirement"
  - "Direct external account, DNS, or credential changes"
next_actions:
  - { p: P0, do: "USER: revoke/rotate the previously exposed Aliyun AccessKey outside this repository" }
  - { p: P1, do: "Observe the next 06:00/07:00 scheduled writers under the new self-check prompts, fail-fast JSON step, and catchup dispatch retry; keep fail-closed if a real write path still fails" }
  - { p: P1, do: "USER: refresh/reinstall the iOS Chrome Home Screen app and confirm the dark-mode status-area surface on device" }
blockers:
  - "Physical-device iOS Chrome status-area confirmation and Alibaba Cloud credential rotation remain external manual actions"
active_session: "ai/sessions/2026-07-20--publish-resilience-hardening.md"
updated: "2026-07-20 11:45 +0800"
---

# MemeDaily State Snapshot

## Current Goal
- Preserve the accepted audit generation while observing the next real dual-feed writer runs and
  closing the two external/manual items: Aliyun credential rotation and physical iOS PWA confirmation.

## 2026-07-20 Incident + Hardening
- 日报 07-20 outage: primary+backup died on agent-emitted malformed JSON, the single catchup that
  fired died on a GitHub API 503 (other hourly catchup schedules were dropped by GitHub), and a
  manual retry died on a 153-char summary (cap 150). Recovered via ADR-006 supervised path: took
  the last run's artifact, ONE faithful trim (中国人民银行→央行), stamp:publish, check green,
  data-only commit `07e12f5`, live on production.
- Root cause behind the malformed-output surge: the 07-14 ADR-006 confinement removed shell from
  the model job, so the agent LOST its `npm run validate` self-check loop; nothing replaced it
  (memes 07-15 + news 07-20 failures all post-date it).
- Hardening (this session, see `ai/sessions/2026-07-20--publish-resilience-hardening.md`):
  (1) catchup dispatch retry ×3 with backoff in both catchup workflows; ACTIVE probe also retried,
  proceeding on persistent API failure; (2) deterministic fail-fast `node JSON.parse` step in both
  publish agent jobs before artifact upload; (3) shell-less self-check ritual in both automation
  MDs + inline prompts (re-Read after every Write/Edit; last action = clean full Read); (4) news
  summary target 100–140字 with the schema cap unchanged at 150; (5) after an adversarial review
  CONFIRMED that a duplicate dispatch queued behind an in-progress publish would re-run the agent
  against a stale pinned checkout (~$5 duplicate + red add/add rebase), both publish dedup guards
  now read today's envelope from the LIVE origin/main tip (anonymous fetch ×3, worktree fallback)
  — a queued duplicate genuinely no-ops, which also closes the pre-existing manual-dispatch hole.
  ADR-006 amended; schema.ts summary comment + memes tier/turn prompt nits fixed.

## Current Architecture
- Next.js static export serves two git-backed feeds at `https://memedaily.fun`:
  memes in `data/daily/` and everyday-life news in `data/daily-news/`.
- GitHub Actions runs the primary cloud publishers at about 06:00 (news) and 07:00
  (memes), with catch-up, fallback, monitor, CI, and Pages workflows.
- The model-facing jobs may write only the exact dated JSON artifact and cannot run
  shell commands. A separate trusted job stamps publication time, validates, commits,
  and pushes. See ADR-006.
- Local Codex publishing is a supervised recovery path, not a mechanically isolated
  unattended publisher. Its rules live in `ai/prompts/CODEX_FULL_HANDOFF.md`.
- Collection is public-web intelligence only. The project stores compact source
  metadata, never login cookies, copied media, or comment archives.

## Verification Status
- Installed Skills were deduplicated into 524 canonical names; 23 primary Skills and 26 covered
  duplicates are applicable to the Next.js/static-data/PWA/GitHub Actions project.
- All important tracked source, workflow, script, configuration, data, prompt, documentation,
  and production-asset types have been read; baseline `npm run check`, strict governance gates,
  secret scanning, dependency integrity, action pin verification, and source-link probes passed.
- Confirmed repair targets are limited to post-rebase validation, monitor issue isolation,
  latest DailyNews run state, required source outlets, stale fallback labels, clipboard feedback,
  PWA-test complexity drift, and canonical collaboration-document drift.
- Confirmed defects are repaired. Final `npm run check` validates 25 meme days, 15 news days,
  94 tests, and 127 static pages; five strict governance gates, all shell/YAML parsing, npm audit,
  lock integrity, React Doctor changed-scope, and 37-warning ADR complexity baseline pass.
- Final Playwright checks pass on 390px dark and desktop surfaces with no console/request errors or
  horizontal overflow; tabs, archive search, detail copy feedback, and 404 behavior pass. Archive
  prefetch is zero. Lighthouse is mobile 77/100/100/63 and desktop 98/100/100/63; SEO reflects
  intentional `noindex`.
- PR #32 checks passed and merge commit `5213cda` passed CI run `29313828480` and Pages run
  `29313828427`. Production apex/www HTTPS, manifest/SW/offline/fonts, archive/detail routes,
  mobile dark-mode surface, keyboard tabs, clipboard feedback, service-worker control, console,
  and request logs all pass.
- Main fallback no-op smoke runs `29313998364` and `29314006065` passed without changing data or
  invoking the token-scoped push step. The next true writer commit remains an operational watch item.

## Persistent Risks
- Final status-area rendering is controlled partly by the installed iOS/WebKit container and
  requires one physical-device confirmation after Chrome refreshes the app manifest/cache.
- A previously shared Aliyun AccessKey was re-enabled outside the repository. Rotate or
  revoke it in Alibaba Cloud; secret scanning cannot invalidate an already exposed key.
- Historical Claude design exports under `docs/project/` intentionally retain generated code and
  external prototype concerns; they are non-production artifacts and remain excluded from runtime
  gates instead of being silently rewritten.
- Public-source semantics require editorial judgment. Deterministic gates deliberately
  cover high-signal terms and cannot prove every archived item is contextually safe.
- GitHub Pages and this public repository are not private storage. Never commit secrets,
  private company data, login state, or unpublished source material.

## Canonical Pointers
- Current plan: `docs/20-plan/current-iteration.md`
- Project map: `docs/00-context/PROJECT_MAP.md`
- Product contract: `docs/10-spec/memedaily-product-spec.md`
- Daily rules: `ai/prompts/MEMEDAILY_DAILY_AUTOMATION.md` and
  `ai/prompts/DAILYNEWS_DAILY_AUTOMATION.md`
- Active session: `ai/sessions/2026-07-14--full-skills-audit.md`
- Final audit: `docs/40-audits/2026-07-14-full-skills-audit.md`
