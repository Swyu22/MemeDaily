---
# Machine-readable project state. Field semantics: docs/00-context/STATE_SCHEMA.md
goal: "Match the Chrome-installed PWA system status region to the page's pure-white surface"
tier: feature
scope_in:
  - "iOS standalone metadata and viewport containment"
  - "Chrome manifest/theme metadata and installed-shell cache refresh"
  - "Runtime and offline surface-color alignment"
  - "Sticky header painting, root overscroll control, and mobile verification"
scope_out:
  - "Unrelated visual redesign or feed/content changes"
  - "Native application packaging"
  - "External account administration"
next_actions:
  - { p: P1, do: "USER: reinstall the Chrome PWA after deployment and confirm that the former gray status strip is pure white" }
  - { p: P0, do: "USER: rotate or revoke the Aliyun AccessKey that was previously shared and later re-enabled; do not reuse it for this public repository" }
blockers: []
active_session: "ai/sessions/2026-07-13--pwa-safe-area-fix.md"
updated: "2026-07-13 23:55 +0800"
---

# MemeDaily State Snapshot

## Current Goal
- Preserve viewport containment while removing the visible color boundary between Chrome's
  system-owned status region and the application's main white surface.

## Current Status
- Root cause confirmed: standalone metadata did not request a full safe-area viewport, while
  the sticky header deliberately blurred scrolling content through a translucent background.
- The follow-up switches iOS from the black translucent overlay to its light default status
  surface. `viewport-fit=cover`, the explicit inset, and the opaque header remain in place.
- Local mobile acceptance confirms `html`, `body`, header, theme, launch background, and every
  simulated top-safe-area pixel resolve to `#fafafa`; sticky geometry still aligns at 133px.
- `npm run check` passes both validators, lint, typecheck, 80 tests, and the 124-page build.
- PR #22 merged as `295e0d0`; Pages run `29258153928` passed. Production now serves the
  `default` status style and `#fafafa` page/theme/manifest surfaces.
- Production mobile acceptance found zero mismatched safe-area pixels after scroll, no blur,
  and exact 133px header/tab-strip alignment.
- Physical Safari is now correct, but the user reports that Chrome still composites scrolling
  content into the top region.
- The Chrome follow-up disables root vertical overscroll, adds a fixed safe-area guard, forces a
  light browser color scheme, versions the manifest URL, gives it a stable `id`, and bumps the
  installed-shell cache to `memedaily-v3`.
- Chromium mobile acceptance under dark system preference passed the full manifest/theme contract,
  fixed 47px guard, 133px sticky alignment, disabled blur, and zero mismatched top pixels.
- `npm run check` passes both validators, lint, typecheck, 80 tests, and the 124-page build.
- PR #24 merged as `f9ce590`; Pages run `29259440314` passed. Production serves the versioned
  manifest, stable app `id`, light scheme, and `memedaily-v3` shell.
- Production Chromium acceptance repeated the full contract with 200 manifest/SW responses,
  overscroll `none`, fixed 47px `#fafafa` guard, 133px sticky alignment, no blur, zero mismatched
  top pixels, and no hard first-party request failures.
- Physical Chrome disproved the simulated guard approach: `viewport-fit=cover` still gave the
  browser permission to composite scrolling content into its system region, while Chrome did not
  expose a usable top inset for the CSS guard.
- The corrective implementation now uses `viewport-fit=contain`, removes the synthetic safe-area
  padding/guard, keeps root overscroll disabled, and aligns document, header, theme, manifest, and
  launch surfaces to `#fafafa`. The manifest URL is versioned `20260713-2` and the Service Worker
  cache generation is `memedaily-v4` so a newly installed shell cannot reuse the prior metadata.
- Local build emits the containment contract. Chromium at 390x844 under a dark system preference
  confirms opaque `#fafafa` surfaces, no blur, no safe-area overlay, 86px sticky alignment after
  scroll, 200 manifest/SW responses, and no failed first-party requests.
- `npm run check` passes both data validators, lint, typecheck, all 80 tests, and the 124-page
  static build with the containment change.
- PR #26 passed both governance checks and merged as `aa1df95`; CI run `29262548831` and Pages
  run `29262548127` both passed.
- Production serves the containment metadata, versioned manifest, and `memedaily-v4` worker.
  Chromium production acceptance confirmed the 86px sticky alignment, zero non-`#fafafa` pixels
  in the unobstructed top scan, no hard first-party failures, two July 13 cards, and the audited
  `谢停风` detail remaining unavailable.
- Physical Chrome confirms containment stopped the scroll bleed, but exposed the configured
  `#fafafa` application surface as a visible light-gray status strip beside white content.
- The follow-up changes the runtime root, document theme, manifest launch/theme colors, and offline
  shell to pure white `#ffffff`, while retaining `viewport-fit=contain`, opaque painting, and
  disabled root overscroll. Manifest generation `20260713-3` and worker `memedaily-v5` prevent an
  installed shell from silently retaining the former gray contract.
- A static PWA surface regression test now binds those cross-file values together.
- Local Chromium at 390x844 under a dark system preference confirms pure-white root/body/header
  surfaces, zero mismatched pixels across the unobstructed top scan, no blur or synthetic overlay,
  disabled overscroll, exact 86px sticky alignment, and 200 manifest/worker responses.
- `npm run check` passes both data validators, lint, typecheck, 82 tests, and the 124-page build.
- PR #28 passed both governance checks and merged as `bb92389`; CI run `29264121356` and Pages
  run `29264118260` both passed.
- Production serves `theme-color=#ffffff`, manifest generation `20260713-3`, pure-white launch
  colors, and `memedaily-v5`. Chromium production acceptance repeated the zero-mismatch top scan,
  opaque white surfaces, disabled overscroll, no blur/overlay, exact 86px sticky alignment, and no
  hard first-party request failures.
- The July 13 publisher did create three visible memes at 07:37 +0800 in commit `ecbc3a5`.
  PR #17's deterministic safety audit changed `谢停风` to `published:false` at 20:24 +0800 in
  commit `e45be31`, because its typhoon/disaster context matched the product's explicit safety
  backstop. The item remains in `data/daily/2026-07-13.json` for traceability; the public selector
  correctly shows the two remaining `published:true` records.
- `npm run check` passes 80 tests and builds 124 pages. A 390x844 browser acceptance with a
  forced 47px inset passed before/after-scroll geometry and found zero polluted safe-area pixels.
- PR #20 merged as `9e2350a`; CI and Pages run `29256033620` passed. Production serves the new
  viewport/status-bar metadata, manifest colors, opaque header, and 200 responses for `/`,
  `/archive/`, and `/sw.js`.
- Production repeated the 47px inset test at `scrollY=1200`: header and tab strip aligned at
  133px, the blur remained disabled, and the safe-area scan again found zero polluted pixels.

## Current Architecture
- Next.js static export serves two git-backed feeds at `https://memedaily.fun`:
  memes in `data/daily/` and everyday-life news in `data/daily-news/`.
- GitHub Actions runs the primary cloud publishers at about 06:00 (news) and 07:00
  (memes), with catch-up, fallback, monitor, CI, and Pages workflows.
- The model-facing jobs may write only the exact dated JSON artifact and cannot run
  shell commands. A separate trusted job stamps publication time, validates, commits,
  and pushes. See ADR-006.
- Local Codex publishing is a supervised recovery path, not a mechanically isolated
  unattended publisher. Its rules live in `ai/prompts/CODEX_FULL_HANDOFF.md`.
- Collection is public-web intelligence only. The project stores compact source
  metadata, never login cookies, copied media, or comment archives.

## Audit Status
- Core repairs and local acceptance are complete across security, workflow confinement,
  temporal integrity, deterministic meme safety, accessibility, PWA caching, performance,
  and governance gates.
- Historical published envelopes were normalized so generation/source timestamps do
  not claim knowledge after publication. High-signal unsafe meme records were hidden.
- `npm run check` passes 80 tests and builds 124 pages; strict checks, Playwright desktop /
  mobile / offline acceptance, font integrity, npm audit, and Lighthouse have passed.
- PR #17, both remote governance checks, Pages run `29250003804`, HTTPS routes, hidden
  content, and production mobile browser acceptance passed at merge SHA `1c04ed8`.
- PR #18 upgraded official Actions to reviewed, full-SHA-pinned Node 24 releases. Both
  governance checks and warning-free Pages run `29250488371` passed at SHA `fd8ef5e`.
- All repository-scoped audit, repair, verification, sync, and production work is complete.
- Detailed findings, repairs, acceptance evidence, and residual risks are recorded in
  `docs/40-audits/2026-07-13-full-skills-audit.md`.

## Persistent Risks
- A previously shared Aliyun AccessKey was re-enabled outside the repository. Rotate or
  revoke it in Alibaba Cloud; secret scanning cannot invalidate an already exposed key.
- Public-source semantics require editorial judgment. Deterministic gates deliberately
  cover high-signal terms and cannot prove every archived item is contextually safe.
- GitHub Pages and this public repository are not private storage. Never commit secrets,
  private company data, login state, or unpublished source material.

## Canonical Pointers
- Current plan: `docs/20-plan/current-iteration.md`
- Project map: `docs/00-context/PROJECT_MAP.md`
- Product contract: `docs/10-spec/memedaily-product-spec.md`
- Daily rules: `ai/prompts/MEMEDAILY_DAILY_AUTOMATION.md` and
  `ai/prompts/DAILYNEWS_DAILY_AUTOMATION.md`
- Active session: `ai/sessions/2026-07-13--pwa-safe-area-fix.md`
