---
# Machine-readable project state. Field semantics: docs/00-context/STATE_SCHEMA.md
goal: "Complete and verify the Codex Cloud takeover of both daily feeds with trusted candidate publication"
tier: milestone
scope_in:
  - "Codex Cloud primary, catch-up, monitor, and fallback schedules for MemeDaily and DailyNews"
  - "Trusted one-file candidate ingestion, CI/Pages hardening, production alignment, and operational evidence"
  - "Data validation, browser payload minimization, detail metadata, prompts, ADR, plan, and session state"
scope_out:
  - "Unrelated visual redesign, historical audit rewrites, private-platform extraction, and paid model APIs in the product"
  - "Destructive external credential changes or private account administration unrelated to the takeover"
next_actions:
  - { p: P0, do: "Merge the final wait-budget/spec correction, restore and API-verify codex-trusted-main, then prove the DeployKey path" }
  - { p: P0, do: "Run final integrated checks and prove local/main/Pages/production alignment" }
  - { p: P1, do: "Exercise one real Codex candidate or skipped fallback and record idempotency/deployment evidence" }
  - { p: P1, do: "USER: revoke/rotate the previously exposed Aliyun AccessKey outside this repository" }
  - { p: P2, do: "USER: refresh/reinstall the iOS Chrome Home Screen app and confirm the dark-mode status area on device" }
blockers:
  - "Aliyun credential rotation and physical-device iOS confirmation remain external manual actions"
active_session: "ai/sessions/2026-07-25--codex-cloud-publisher-takeover.md"
updated: "2026-07-25 23:00 +0800"
---

# MemeDaily State Snapshot

## Current Goal

- Finish the account-level Codex Cloud schedule setup, merge the trusted publishing
  architecture, and verify a real daily candidate through GitHub main and production.

## Reconciled Baseline

- Local `main` was fast-forwarded to remote commit
  `47e16e1c1b30a814982c17883cbd046bc37aa323`.
- Before changes, that same SHA was the GitHub `main` tip and latest successful Pages
  deployment; `https://memedaily.fun/` returned HTTP 200 from that generation.
- During the maintenance window, the still-live legacy news fallback legitimately
  added `data/daily-news/2026-07-25.json` as `skipped` at remote main `d783d8e`;
  the feature branch will rebase onto that data-only commit before publication.
- PR #37 merged the takeover at `fbccae3`; its main CI and Pages run succeeded.
  A final review correction proceeds on `codex/fix-cloud-rollout-contract`.

## Current Architecture

- Next.js static export serves two git-backed feeds at `https://memedaily.fun`:
  memes in `data/daily/` and everyday-life news in `data/daily-news/`.
- ChatGPT Work / Codex Cloud owns unattended schedules and reads current rules/data
  from GitHub on every run. It submits one exact daily JSON on
  `codex/daily-{meme|news}-YYYY-MM-DD` and opens a same-repository PR.
- `codex-daily-pr-publish.yml` never checks out the PR tree. A read-only job fetches
  only the JSON blob and validates with trusted main code; a separate trusted job
  stamps, revalidates, rebases, pushes, and waits for correlated Pages success.
- Repository ruleset `codex-trusted-main` is the credential boundary: Cloud-connected
  tools and ordinary users cannot update/merge main; one Actions-secret-backed write
  deploy key is the sole bypass and appears only in trusted final push steps.
- Old Anthropic publishers, GitHub cron, catch-up dispatchers, and external cron
  ownership are retired. Fallback/monitor workflows are manual disaster recovery.
- Public-web content is untrusted data. No login cookies, private APIs, anti-bot
  bypass, copied media, private data, or paid model API is part of the product.

## Implemented Hardening

- Production dependency audit is zero after the required Next/PostCSS/Sharp updates;
  CI and Pages enforce the production high-severity gate under Node 22/npm 10.
- Pages build has read-only contents permission; only deploy receives Pages/OIDC writes.
- Pages correlation adopts the deploy-key push run when it appears, falls back to one
  explicit dispatch only when needed, retries transient failures, and returns success
  only for a run covering the accepted SHA (or descendant) with conclusion `success`.
- Its 75-minute per-run correlation budget and 180-minute writer budget cover the
  configured 35-minute Pages job maximum, one predecessor, and one bounded recovery.
- Fallback uses live-main sync, strict date/path containment, shared writer
  concurrency, post-rebase full checks, and an explicit `HEAD:main` push.
- Monitors read live main and verify Pages freshness; empty DailyNews input now fails.
- Reader client payloads use a public meme projection; internal editorial fields remain
  server/repository only. Detail pages now expose canonical/OG/Twitter metadata.
- ADR-007 and `ai/prompts/CODEX_CLOUD_RUNBOOK.md` define the active operational contract.

## Verification Status

- Eight dedicated ChatGPT Work Cloud contexts passed read-only GitHub/main checks and
  each owns one active Asia/Shanghai heartbeat trigger group.
- Node 22.23.1/npm 10.9.8 full verification passes: production audit reports zero,
  validators/lint/typecheck pass, 115 tests pass, and 176 static pages build.
- Strict all-repository and staged governance gates, workflow YAML parsing, shell
  syntax, diff whitespace, and 28 focused security/reliability/data tests pass.
- All retained workflow YAML parses and `scripts/dispatch-pages.sh` passes shell syntax.
- Independent subtask verification passed typecheck, strict lint, 113 tests, a
  176-page static build, production audit, governance checks, and targeted output
  inspection before final integration.
- Ruleset `codex-trusted-main` was activated after PR #37 and API-verified with only
  a DeployKey bypass. Final maintenance CI/Pages, restored-protection verification,
  and the real Cloud candidate acceptance run remain open.

## Persistent Risks

- Connected GitHub tools retain branch/PR permissions, but the main ruleset blocks
  their direct-main/self-merge path. First scheduled runs and connector permissions
  still require observation as defense in depth.
- Public-source semantics require editorial judgment; deterministic gates cover
  high-signal failures but cannot prove every contextual decision.
- A previously shared Aliyun AccessKey must be revoked/rotated outside the repository.
- Final iOS installed-PWA status-area rendering still needs physical-device confirmation.
- GitHub Pages and this public repository are never private storage.

## Canonical Pointers

- Active plan: `docs/20-plan/current-iteration.md`
- Project map: `docs/00-context/PROJECT_MAP.md`
- Cloud runbook: `ai/prompts/CODEX_CLOUD_RUNBOOK.md`
- Living rules: `ai/prompts/MEMEDAILY_DAILY_AUTOMATION.md` and
  `ai/prompts/DAILYNEWS_DAILY_AUTOMATION.md`
- Decision: `docs/30-decisions/adr-007-codex-cloud-candidate-publishing.md`
- Active session: `ai/sessions/2026-07-25--codex-cloud-publisher-takeover.md`
