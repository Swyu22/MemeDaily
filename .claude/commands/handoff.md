---
description: 完整交接——新模型/新会话接手既有项目的对齐流程
argument-hint: [任务类型 + 目标模块/文件（可选）]
---
按 @ai/prompts/NEW_MODEL_HANDOFF_PROMPT.md 的完整流程接手本项目：先「读取与对齐」，再「输出理解（≤8 条）」，再「执行计划」，通过后才进入执行。

可用 context-loader 子代理完成渐进式读取。

本次任务类型与目标模块（如已知）：$ARGUMENTS
